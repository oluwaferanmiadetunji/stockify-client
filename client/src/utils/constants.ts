export const ROUTES = {
  LOGIN: '/auth/login',
  DASHBOARD: '/app/dashboard',
  ERROR: '/404',
}

export const API_URL = process.env.REACT_APP_API_URL

export const API_ROUTES = {
  LOGIN: '/auth/login',
}
